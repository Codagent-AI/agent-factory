## REMOVED Requirements

### Requirement: Sync the working clone after merge

**Reason**: Post-merge sync and issue closure apply to every pull-request claim, fix or feature.
**Migration**: Replaced by "Sync the working clone and close the issue after merge" in `factory-pull-request-lifecycle`; fix behavior is unchanged.
