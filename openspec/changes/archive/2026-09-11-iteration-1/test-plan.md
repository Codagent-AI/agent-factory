## Coverage Strategy

Specifications remain the source of unit-test requirements. This plan records only additional integration, end-to-end, agent-acceptance, and exceptional human-only obligations. Coverage was approved using the categories and dependency boundaries below, with one full live and-scene repetition during acceptance.

| Category | How it runs | Dependency boundary |
|---|---|---|
| Integration tests (`INT-*`) | Automated tests connect production components under controlled conditions | Real temporary SQLite and files; stub external GitHub, process/suite, and clock responses where specified |
| E2E tests (`E2E-*`) | Automated journeys through the delivered CLI/service entry points | Real Git, filesystem, SQLite, operating-system processes, and Docker where specified; a controlled executable replaces model work and GitHub uses a local stub |
| Agent acceptance (`AT-*`) | An agent manually operates the delivered product and checks observable results | Real GitHub, deployed routing workflow, installed Mac service, and actual eval suite; no model/suite substitute for the live evaluation |
| Human-only (`HT-*`) | Operator performs the one required board drag; the agent checks its API effect | Real GitHub board and production Project reads; no model execution |

The E2E tests prove the factory's local behavior; they do not claim to cover real GitHub or model-provider integration. The required agent acceptance flows supply that coverage. Most failure combinations belong in repeatable controlled tests, not additional paid evaluations.

Prioritize duplicate-execution prevention, durable progress/reporting, correct routing and author authorization, pinned revisions, and operational control. Do not duplicate the full specification's validation/decision cases across these layers. No testing of future kinds/suites, distributed workers, a remote database, arbitrary completed-card gestures, or subjective product quality is introduced.

Automated tests use isolated roots and temporary data. They must not read the operator's factory database, private credentials, or live queue. Stub clocks and configurable short limits avoid real five-hour quota waits or overnight sleeps. Process assertions use bounded readiness/exit observations rather than arbitrary sleep durations.

Put automated obligations under `tests/integration/` and `tests/e2e/`, with shared fixtures where useful. Run them with `uv run pytest tests/integration tests/e2e` as part of implementation verification; execute the Mac-specific process coverage on macOS and the Docker compatibility coverage on a Docker-enabled host. Register any test markers explicitly; Docker-dependent obligations may run as a separate required check rather than silently skip on hosts without Docker. The scaffold's current tolerance of pytest exit code 5 is not evidence that these obligations pass: required tests must be collected and run. A platform-specific omission must be reported and completed on its required platform before declaring that obligation satisfied.

## Integration Tests

### INT-001: SQLite lifecycle, continuation, and delivery progress

- Covers: Persist accepted work/history; freeze inputs; continuation versus fresh request; bounded recovery; quota and admission holds; durable pause and reporting; separate execution/product outcomes.
- Boundary: Production controller/handler/store components with a real temporary SQLite database; stub GitHub observations, suite observations, and time.
- Setup: One accepted multi-repetition claim with frozen settings, a completed first repetition, and an unfinished second repetition. Use realistic versioned suite outcome fixtures and named admission conditions. Open fresh store/controller instances to represent restarts rather than merely resetting an in-memory object.
- Action: Continue after a technical interruption; exhaust the one permitted recovery retry; separately defer for Codex quota and reopen the database before reset. Exercise an unchanged continuation, an explicit fresh request, and missing Verdict delivery. Reconstruct pending report progress after restart. Apply pause/window/prerequisite decisions between units.
- Assertions: Completed repetitions and frozen inputs survive; a technical retry is charged only once and exhaustion leaves later units unstarted; quota continuation preserves its original reset and does not consume that retry; pause and holds remain independent. A changed parsed request or cleared successfully reported Verdict can create a fresh claim only when eligible, while formatting or undelivered reporting cannot. Product failure remains distinct from execution failure. A suite-confirmed non-resumable workflow failure settles only its repetition, preserves the separate product verdict, and allows the next repetition; a resumable workflow failure consumes the normal bounded recovery budget. No run is created for a pre-launch readiness hold. Reopening storage retains the intended delivery/event identities.
- **Constraints:** Branch movement and OS process lifetime are covered by E2E tests; this obligation does not mock SQLite or enumerate every score/parser branch.
- Execution: `tests/integration/`; ordinary automated checks on supported development/CI hosts, with no network or model credentials.

### INT-002: Routing and reporting against a controlled GitHub API

- Covers: Shared routing configuration; author permission gate; initialize fields once; manual queue order; invalid-request feedback; Status corrections; cancellation precedence; comments and field-update recovery.
- Boundary: Production configuration, routing/controller code, and GitHub client serialization against a stub transport or local HTTP service; real temporary SQLite for controller reporting. Stub responses must follow the REST/GraphQL shapes actually used by the implementation.
- Setup: Paginated Project items in deliberate manual order; trusted and untrusted authors; valid and invalid eval bodies; routing receipts; issue comments with stable markers. Include configuration for an organization/repository/field mapping other than Codagent to detect hard-coded deployment assumptions.
- Action: Deliver the same routing event twice and interrupt initialization between field updates. Lose the response to a successful comment write, restart delivery, and paginate to find that comment. Change current board state between deliveries, including closure. Exercise a Ready card moved while execution is active and a non-factory card. Supply missing/denied author-permission responses and verify generated calls when App authentication refreshes.
- Assertions: Authorized evals route to Ready even with invalid settings; outsiders remain Backlog without ownership, and unknown permission never authorizes execution. Repeated routing preserves subsequent human changes. Source rules and manual position drive selection, not age/Priority. Corrections affect only the approved factory-owned cases; closure overrides a pending Running repair. A successful lost-response comment is found and not duplicated. Unknown input is reported once until it changes; correction removes the attention label. Authentication is kept out of request bodies, log output, suite environment, and command arguments.
- **Constraints:** No actual organization mutations, Actions runs, or credential rotation. Real installation-token permissions and workflow event wiring are proven by AT-001; no second live outside-contributor account is required for this controlled authorization coverage.
- Execution: `tests/integration/`; ordinary automated checks. Capture API requests as structured values so assertions verify methods, identifiers, and payload semantics rather than shell formatting.

### INT-003: Suite adapter commands, progress, and result contracts

- Covers: Accepted execution settings; suite-specific readiness and resume; progress/runtime accounting; quota interpretation; provenance; result/score reporting and human-review command generation.
- Boundary: Production eval handler and and-scene adapter reading real temporary artifact files; controlled process/clock responses and representative versioned artifacts from the deployed suite interface.
- Setup: Fixtures for automated completion ready for review, a completed 39/70 product failure, exactly 40/70 with eligible outcome, incomplete scoring, and product evidence followed by a technical error. Include actual structured suite fixtures for a non-resumable workflow-side-effect violation, a resumable workflow failure, and a recoverable harness failure. Include known suite log/state updates, a bounded Claude wait, a recognized Codex rate-limit diagnostic with a reset, the same recognized limit without an interpretable reset, and an unrelated execution error. Source Codex detection fixtures from captured or authoritative documented CLI diagnostics and record their source; invented parser examples do not establish compatibility. Use paths containing spaces and shell metacharacters as ordinary argument data.
- Action: Build initial, valid-checkpoint resume, and proven pre-checkpoint retry execution plans; reject corrupt or unexplained missing checkpoint states; advance logs/state while stdout stays quiet; enter/leave recognized quota waits; read results and aggregate mixed repetition outcomes; render review commands.
- Assertions: Arguments carry all accepted profiles, worktrees, environment-file references, validator setting, and stable artifact identity without shell evaluation. Resume retains the same inputs and path. A proven pre-checkpoint interruption retries without `--resume` under the same budget; corrupt or unexplained missing state never falls back to a fresh run. Readiness and invocation work without a calibration receipt and pass no removed calibration-record argument. Known artifact activity counts as progress; bounded quota waits affect the correct timers; restart reconstruction does not reset timing. The adapter consumes suite-established failure and never calculates a substitute threshold or treats missing scores as zero. Retry exhaustion and completed product failures produce the required distinct board outcomes. A non-resumable workflow failure settles its repetition and permits later repetitions, contributing board Verdict failed without changing an unavailable product verdict; a resumable workflow failure keeps the bounded retry. Only recognized Codex rate limits create holds; the five-hour fallback requires a recognized limit and generic errors follow ordinary recovery. Eligible repetitions get correctly quoted absolute review commands even in a mixed-result claim; failed/incomplete repetitions do not. Image provenance is recorded from observed execution, not fabricated before build.
- **Constraints:** Fixtures must preserve real suite field names and distinctions; a fixture mismatch is an integration failure, not a reason to relax the result contract. This does not re-test the suite's scoring algorithm. The companion linked-worktree mount change is verified separately by E2E-004.
- Execution: `tests/integration/`; ordinary automated checks without Docker, model calls, or candidate PR creation.

## End-to-End Tests

### E2E-001: Real Git worktrees through factory admission and continuation

- Covers: Clean pinned worktrees; frozen inputs; distinct claim/repetition identity; continuation and fresh requests; configurable repository paths; worktree cleanup after review reaches Done.
- Surface: Delivered factory CLI, including `tick`, operating through the normal admission path.
- Setup: Isolated factory root and SQLite; real temporary Git repositories/remotes representing Runner, Skills, and the deployed harness, with the minimal supported fixture layout. Local GitHub stub supplies the request. A controlled executable stands in for the suite and records its received arguments; Git, worktree creation, revision resolution, and persistence are real.
- Journey: Admit a request, finish one unit, defer the remaining unit, advance the source branches and configured defaults, and continue the same claim. Then admit a separate fresh request. Finish the first claim and observe retention in Review, move its reviewed card to Done in the GitHub stub, and tick again. Exercise a partial cleanup failure using a test-owned locked Git worktree, restart the controller, remove that lock, and retry while another eligible job can proceed.
- Assertions: The initial claim uses clean detached worktrees at its original SHAs throughout continuation. Source working checkouts remain unchanged. A second claim has separate worktrees and can resolve the newer inputs without repointing the first. Repetitions have distinct Git-ref-safe artifact identities, while a resumed unit retains its path. Reported pins agree with the actual worktrees. Shared configuration changing outside the installed version does not silently change the running installation. Worktrees survive waits and Review, then disappear from disk and Git worktree registrations after reviewed Done is observed. Results, logs, SQLite history, candidate refs, and other claims/source checkouts remain. Failed cleanup persists across restart and retries without blocking other jobs; already-removed worktrees are tolerated. Verify candidate PR preservation through the stub API: cleanup sends no PR deletion or closure requests.
- Execution: `tests/e2e/`; automated checks on hosts with Git installed. No GitHub network, Docker, or model calls. Include interruption before the controlled suite creates its checkpoint and verify recovery uses the same repetition without `--resume` and consumes only the existing retry budget. Clean only the test-owned repositories/worktrees after assertions.

### E2E-002: Real processes survive controller failure without duplicate execution

- Covers: Independent supervisor lifetime; single execution across service/tick; interrupted startup; real execution recovery; persisted timers and cancellation; ownership verification.
- Surface: Delivered controller/service process plus CLI commands, with real supervisor and child processes.
- Setup: Isolated SQLite, locks, logs, and factory configuration; local GitHub stub. A long-running controlled suite executable exposes durable progress and records each launch. Give the test runner explicit ownership of all created processes. Use short configured limits for timeout variants.
- Journey: Start an attempt and terminate only its controller. Restart the controller and issue a concurrent tick. Separately interrupt the supervisor while its child survives, finish a child while the controller is absent, and interrupt execution itself to exercise bounded recovery. Cover the reserved-start interval using synchronization points, not probabilistic timing. Request cancellation of test-owned execution and retain an unrelated decoy process.
- Assertions: Controller restart leaves the same execution alive, retains elapsed accounting, and causes no new attempt/retry. Concurrent entry points do not overlap. Supervisor replacement observes a survivor without relaunching it. Durable completion recovers reporting without rerunning. Actual interruption alone triggers the specified recovery policy. An uncertain launch/identity holds the slot. Cancellation/timeout affects only owned execution; the decoy survives.
- **Constraints:** The suite executable is a declared substitute for agent/model work; controller, supervisor, locks, SQLite, and process/session boundaries must be real. This is not proof of Docker mount ownership; E2E-004 proves it using controlled real containers, and AT-002 records ownership of the real suite container. Do not kill or stop unrelated host processes/containers to create faults.
- Execution: `tests/e2e/`; required on macOS for the supported deployment's process semantics. Compatible portions may also run on other CI hosts. All test-owned processes must be stopped/reaped in cleanup. Actual launchd restart behavior is additionally observed in AT-002.

### E2E-003: Installed CLI controls and reporting across a short multi-repetition journey

- Covers: Public doctor/status/tick/pause/resume behavior; admission between repetitions; readiness holds; manual ordering and status reconciliation; result and review-command delivery.
- Surface: Installed `agent-factory` commands, invoked as subprocesses rather than direct internal function calls.
- Setup: Isolated root and database, local GitHub stub with at least two candidate cards, controlled suite executable writing representative artifacts. Use real processes and files with short configured scheduling/supervision intervals and a controllable test clock where needed. One request has multiple short repetitions.
- Journey: Diagnose one unavailable prerequisite, repair that test fixture, admit work, pause while a repetition runs, finish it, and reopen the CLI before resuming. Reorder queued cards and change the active card to Ready. Deliver a final result and recover an interrupted report. In a separate controlled path, close the active issue to cancel it.
- Assertions: Doctor explains readiness without launching; status is useful during execution and waiting. Tick obeys holds and the single-run guard. Pause survives process exit and permits only the current unit to finish; resume leaves unrelated holds intact. Next selection follows manual position. A contradictory Running/Ready edit is corrected without another attempt. Also drag active execution to Done and verify Running is restored and its worktrees remain. Closure cancels owned work. Final issue/board payloads include correct outcome, pins, evidence paths, and an executable-path review command when eligible; no official pass or automatic issue closure is generated.
- **Constraints:** Do not wait overnight or exhaust real quota. Scheduling arithmetic belongs to unit tests; this journey proves the controls are wired through the public CLI and persistence. No live network/model dependencies are claimed.
- Execution: `tests/e2e/`; automated checks on supported development/CI hosts. Commands must return observable exit status/output and clean up test-owned processes and storage.

### E2E-004: Real Docker linked-worktree mounts, provenance, and ownership

- Covers: Companion suite-wrapper mount support; selected Runner launcher compatibility; in-container Git provenance; exact container ownership and immutable image identity.
- Surface: Delivered suite/Runner sandbox wrapper path and factory container observation/termination code, using real Docker and Git. A controlled command replaces agent/model execution.
- Setup: Test-owned detached linked Runner/Skills/harness worktrees at the selected deployment commits, isolated artifacts, an explicitly test-owned image tag supplied through the launcher's `--image` option, and a Docker-enabled host. Use the actual suite wrapper mount construction and actual selected Runner launcher, not a separately invented Docker command claiming to test their compatibility. Expose the wrapper's command assembly to this controlled test if needed. Create a second test-owned decoy container from the same image with a different artifact mount.
- Journey: Start the controlled command through the wrapper/launcher combination. Inside its container, run Git revision and cleanliness checks against the mounted source worktrees and confirm the expected pins. Observe container identity and `.Image`, retag the test-owned image to a different test image, then stop only the recorded owned container using the factory's ownership checks. Exercise a contradictory artifact mount and verify termination is refused.
- Assertions: Backing Git metadata is accessible read-only; provenance checks succeed without rewriting `.git` or changing host source checkouts. Source and metadata mounts remain read-only. Recorded image identity matches the running container's immutable ID despite tag movement. Termination requires the recorded container identity and exact artifact mount; the same-image decoy survives. Missing launcher capability is reported as a readiness problem before model execution.
- Execution: `tests/e2e/`; required on a Docker-enabled host. This uses real containers and may build the selected Runner image, but consumes no model calls or GitHub mutations. Remove only test-owned containers, images/tags, and worktrees. Preserve unrelated host resources. Record a missing Docker environment as incomplete coverage, not a pass.

## Agent Acceptance Tests

These are manual product exercises performed by the implementation/acceptance agent, not additional pytest suites. Both flows are required. A tester may group their shared setup but may not substitute mocks or a dry run for their live boundaries. If required credentials, deployed workflows, readiness, or a suitable admission window are unavailable, record acceptance as incomplete until the flow can run.

Complete AT-001 and AT-002 independently of the operator's HT-001 board drag. A pending HT-001 does not block completion of agent acceptance preparation; carry it forward explicitly as a required human-acceptance observation, never as a passed agent test. After both agent flows pass, prepare the separate paused dummy cards described in HT-001 for the operator's later interaction.

The App and Project were provisioned during design; use `setup/github-app.md`, `setup/project-board.md`, and recorded identifiers. Those records prove setup at that time, not implementation acceptance. Routing code must first be published to its referenced revision, and caller workflows/template deployed through the repository's normal approved deployment process. This plan does not authorize bypassing repository review or merging arbitrary code merely to unblock a test.

Use clearly marked acceptance issues and retain enough linked evidence to distinguish them from real queue work. The real author for an automatically queued eval must be an account with repository write access (the configured operator account), not an App-authored issue whose permission was assumed. Factory reporting uses the App; candidate delivery uses the separate suite token. Never include keys or tokens in evidence.

GitHub acceptance may be performed through `gh`/the public API using the delivered template content, labels, and native type, followed by real Actions event delivery. This is the explicitly permitted alternative to browser-driven issue creation; directly invoking routing internals or workflow dispatch in place of the issue-creation event is not equivalent. Preserve the earlier board-layout confirmation; screenshots are required if a tester chooses to exercise or report new UI behavior, while API/CLI flows use their actual client-visible evidence.

### AT-001: Real issue intake, routing, correction, and general-work handling

- Classification: Required
- Covers: Delivered eval template and caller workflow; live App permissions; automatic factory assignment; invalid-request feedback; native Type/Project fields; ordinary work intake and closure.
- Actor and surface: Acceptance agent acting through the authorized operator's GitHub CLI/API or normal GitHub UI, the deployed Actions workflow, and installed factory CLI.
- Setup: Pause execution before creating test requests. Confirm the live board and configured source repositories. Use two marked eval issues in `agent-evals` and one marked ordinary Task issue in a configured source repository. The eval issue initially contains the delivered template with an intentionally invalid repetition count; no real evaluation is admitted in this flow.
- Steps:
  1. Create the eval issue with the template's markers/native type and observe its actual issue-triggered workflow. Do not manually assign Owner or move it to Ready.
  2. Confirm factory ownership and Ready. Run/observe the normal factory cycle while paused; check the needs-input label and corrective comment, with no execution.
  3. Correct the TOML to one repetition and valid configured inputs. Observe removal of needs-input, retaining the pause and zero launches. Keep this corrected issue for AT-002.
  4. Create a second disposable eval request with valid input and observe automatic routing while execution remains paused. Capture its fields and zero-launch evidence, then close/delete it so only the original eval can enter AT-002. No operator interaction is required in this flow.
  5. Create the ordinary Task without an eval marker. Verify Backlog and no factory ownership. Close that Task and observe the delivered closure automation move it to Done.
- Expected: Routing works independently of local admission, template/native type and deployed event wiring agree, invalid input remains a request for correction rather than an infra failure, correction does not bypass pause, and ordinary work is tracked without becoming an eval request.
- Evidence: Issue URLs and authored bodies/markers; actual Actions run URL and revision; before/after field and label values; correction comment URL; CLI status showing paused/no active execution. If using the UI, capture meaningful views of the routed eval and ordinary Task. Record the implementation revision and Project identifier.
- Effects and cleanup: Authorized effects are the three marked issues, their Project items, normal workflow runs/comments/field changes, and closure/deletion of the disposable Task and second eval after evidence is captured. Keep the corrected eval for AT-002. Use the administrator CLI identity for deletion if the App cannot delete issues. This flow consumes ordinary GitHub Actions usage but no eval model calls. Restore only test-owned data; leave unrelated cards and operator settings alone.
- Permitted substitutes: Real GitHub CLI/API issue creation as described above may replace browser interaction. No stub GitHub, synthetic workflow result, or direct routing invocation may replace actual issue-triggered routing.

### AT-002: One real evaluation with a controller restart and final handoff

- Classification: Required

The human-review-command portion applies when the suite returns an eligible result; a valid suite-established product failure has its own expected result below.

- Covers: Installed Mac service, real suite/Docker execution, pinned Runner/Skills inputs, controller restart survival, Status reconciliation, live results and candidate links, and human-review handoff.
- Actor and surface: Acceptance agent manually using the installed CLI, launchd service controls, real GitHub issue/Project APIs, Docker observations, and the suite's delivered artifacts.
- Setup: Use the corrected AT-001 issue with **one requested repetition**, explicit valid Runner/Skills revisions and deployment profiles, on the Mac mini. Confirm doctor/readiness, the suite outcome contract and deployment of the calibration-gate removal (`2fc8443` or its integrated equivalent), Docker/model authentication, and separate candidate-delivery credentials. Use the normal 00:00–15:00 admission window; do not bypass it or inject an internal run. Ensure unrelated requests cannot be unintentionally admitted when resuming. Record the initial pause/configuration state for restoration.
- Steps:
  1. Resume through the public CLI when admission is permitted, then use a normal poll or tick. Observe the same issue become Running with one claim/attempt and suite-owned artifacts.
  2. Once actual suite execution/progress is established, record supervisor, child/container identity, artifact mount, and initial attempt information. Restart only the factory controller using the delivered launchd service operation.
  3. Verify that the same evaluation continues, progress advances, and no new attempt, retry, or duplicate container appeared because of the controller restart. Move this test card to Ready and observe Running restored with a single explanatory correction comment and continued execution.
  4. Allow that repetition to finish through the actual suite. Compare the issue/Project reporting with the saved suite outcome, revision pins, evidence path, duration/cost availability, and candidate links. Verify that the issue remains open and the factory never assigns passed.
  5. For a result ready for human review, verify the posted command names the actual retained script and artifact directory, with usable quoting. The agent may invoke it through its non-scoring startup/readiness boundary and stop before entering human ratings. For a suite-established product failure, verify failed reporting and absence of a misleading human-review command instead.
- Expected: Real execution survives the controller restart under launchd without duplication; the factory restores truthful Status; saved pins match the actual Runner/Skills/harness worktrees; and reporting matches the suite's outcome. A genuine product failure is a valid observed factory result and must not be hidden or turned into a technical retry. A technical failure or missing final report does not establish the completed-evaluation portion of acceptance; use only the specified recovery policy and record incompleteness if it cannot finish.
- Evidence: Request and Actions URLs; factory/deployment and component SHAs; timed before/after controller-restart process/container identities and continued-progress evidence; actual artifact mount; claim/attempt history showing no restart-induced retry; final comment/Project values; suite `result.json` and candidate branch/draft-PR URLs when produced; the posted review command or product-failure reason. CLI/API evidence is sufficient for this flow; do not fabricate UI screenshots or claim a human score.
- Effects and cleanup: Approved cost is one real requested repetition, with the product's normal bounded resume behavior if needed; this is not permission for a matrix or repeated fresh paid evaluations. Normal suite work may build Docker images, consume model usage, and push a candidate branch/create a draft PR in the configured `and-scene` repository. Retain its evidence, pinned worktrees, candidate branch/PR, and result issue in Review for inspection/human review, as required by the product. Do not move this live result to Done merely to test cleanup; the real-Git cleanup journey is covered by E2E-001. Stop any non-scoring review process the agent started, restore the prior operator pause state, and remove only clearly disposable acceptance scaffolding. Do not merge PRs, publish human ratings, or automatically delete suite outputs. List retained artifacts and any remaining manual cleanup in the acceptance report.
- Permitted substitutes: None for the live suite, model calls, GitHub reporting, or launchd restart. The outcome may legitimately be a suite-established product failure; the eligible-human-review branch is then inapplicable, with its conditional rendering contract still covered by INT-003/E2E-003. Existing unrelated result files, prerecorded runs, or a controlled suite executable cannot replace this real request.

## Human-Only Testing

### HT-001: Confirm that a board drag changes dispatch order

- Reason: Prior setup confirmed board layout, but did not establish that moving two eligible Eval cards within the same Ready swimlane changes the Project `POSITION` order read by the controller. Paul has requested CLI/API interaction instead of Chrome automation; this single UI gesture needs an operator.
- Prerequisites: AT-001 and AT-002 have passed. Record the current pause state, then pause admission before creating two fresh, clearly marked dummy eval issues in `agent-evals` using valid request settings. Observe their normal automatic routing to Ready and record their issue/Project IDs and initial order through the production Project client. Keep admission paused while these dummy cards exist; they must never launch an evaluation. Do not reuse or move the retained AT-002 result out of Review.
- Instructions: Ask the operator to drag the lower card above the other in Ready within the Eval group, with field-based sorting disabled. The agent then reads both cards through the production Project client and checks that eligible order reverses. Record the operator's observed UI order and the before/after API evidence; attach a screenshot only if one is available, never fabricate one.
- Required decision or observation: The visible drag order matches the controller's observed eligible order. If it does not, human acceptance of manual ordering remains incomplete and the provider ordering implementation must be corrected. Until the operator performs the drag, report HT-001 as pending human acceptance; this does not invalidate independently passing AT-001/AT-002 evidence. A public API position mutation can additionally exercise ordering, but does not replace proof of the UI-to-API relationship.
- **Cost and cleanup:** The two fresh dummy issues, their Project items, and normal routing workflows/comments/field changes are authorized setup for this human check, additional to AT-001's three issues. No evaluation launches or model calls are authorized. After the operator's observation and before restoring the prior pause state, close/delete both dummy issues and verify that neither remains eligible for admission. If the operator defers the check, keep admission paused and list the retained dummy cards in the handoff; do not resume with them queued. Preserve the AT-002 result and evidence in Review and leave unrelated cards alone.

The factory must deliver a valid human-review handoff, but subjective scoring of the generated product is outside acceptance of this change. The agent does not invent ratings or require Paul to judge a candidate merely to finish factory acceptance. Only the specific drag-to-API contract above needs this human interaction; other API/CLI acceptance flows remain agent-operated.

## Coverage Map

Only requirements/journeys with additional obligations appear here. Other specification cases remain implementation-time unit-test requirements; they are not implicitly waived.

| Requirement or journey | INT | E2E | AT | HT |
|---|---|---|---|---|
| Intake: template, shared routing, author gate, and initialize-once behavior | INT-002 | — | AT-001 | — |
| Intake: invalid input and correction without execution | INT-002 | E2E-003 | AT-001 | — |
| Intake: request eligibility | INT-002 | E2E-003 | AT-001 | — |
| Intake: manual order | INT-002 | E2E-003 | — | HT-001 |
| Intake/execution: frozen settings, real revision pinning, worktree isolation | INT-001, INT-003 | E2E-001 | AT-002 | — |
| Intake/lifecycle: continuation versus fresh claim; preserve completed repetitions | INT-001 | E2E-001, E2E-003 | — | — |
| Lifecycle: single execution, controller/supervisor survival, uncertain startup | — | E2E-002 | AT-002 | — |
| Lifecycle: technical retry exhaustion, quota holds, and persisted timing | INT-001, INT-003 | E2E-002 | — | — |
| Lifecycle: pause, admission between units, and readiness holds | INT-001 | E2E-003 | AT-001, AT-002 | — |
| Lifecycle: contradictory Status edits and closure precedence | INT-002 | E2E-003 | AT-001, AT-002 | — |
| Lifecycle: owned-execution cancellation and timeout | INT-003 | E2E-002, E2E-003, E2E-004 | — | — |
| Execution: suite invocation, resume, artifact identity, and provenance | INT-003 | E2E-001, E2E-004 | AT-002 | — |
| Reporting: separate outcomes, threshold contract, mixed results, aggregate verdict | INT-001, INT-003 | E2E-003 | AT-002 | — |
| Reporting: durable activity, lost-response recovery, human edits | INT-001, INT-002 | E2E-003 | AT-001, AT-002 | — |
| Reporting: exact human-review command and retained evidence | INT-003 | E2E-003 | AT-002 (eligible result branch) | — |
| Operations: configurable deployment, explicit updates, isolated local root | INT-002 | E2E-001, E2E-003 | — | — |
| Operations: worktree cleanup after reviewed Done, retry and evidence preservation | — | E2E-001, E2E-003 | — | — |
| Operations: public CLI and supported Mac service | — | E2E-002, E2E-003 | AT-001, AT-002 | — |
| Operations: live GitHub installation permissions, routing and closure workflows | — | — | AT-001 | — |
